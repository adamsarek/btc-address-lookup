{
	"default_connection": {
		"host": "localhost",
		"port": 5432,
		"user": "postgres",
		"password": "postgres",
		"dbname": "postgres",
		"autocommit": true
	},
	"users": [
		{
			"user": "builder",
			"password": "...",
			"superuser": true
		},
		{
			"user": "client",
			"password": "...",
			"superuser": false
		}
	],
	"databases": [
		{
			"database": "btc_address_lookup",
			"owner": "builder",
			"tables": [
				{
					"table_name": "currency",
					"create": [
						"currency_id SMALLSERIAL NOT NULL PRIMARY KEY",
						"name TEXT NOT NULL UNIQUE",
						"code TEXT NOT NULL UNIQUE",
						"unit TEXT NOT NULL",
						"symbol TEXT NOT NULL",
						"logo TEXT NOT NULL"
					],
					"insert": [
						[
							"name",
							"code",
							"unit",
							"symbol",
							"logo"
						],
						[
							["Bitcoin", "BTC", "bitcoin", "&#8383;", "/src/img/BTC.svg"]
						]
					]
				},
				{
					"table_name": "address",
					"create": [
						"address_id BIGSERIAL NOT NULL PRIMARY KEY",
						"currency_id SMALLINT NOT NULL REFERENCES currency (currency_id) CHECK (address_id > 0)",
						"address TEXT NOT NULL UNIQUE"
					]
				},
				{
					"table_name": "source",
					"create": [
						"source_id SMALLSERIAL NOT NULL PRIMARY KEY",
						"roles SMALLINT[] NOT NULL",
						"name TEXT NOT NULL UNIQUE",
						"last_downloaded_at TIMESTAMP"
					],
					"insert": [
						[
							"roles",
							"name"
						],
						[
							["{}", "LoyceV"],
							["{}", "BitcoinAbuse"],
							["{}", "CheckBitcoinAddress"],
							["{}", "CryptoBlacklist"],
							["{}", "Bitcoin Generator Scam"],
							["{}", "BitcoinAIS"],
							["{}", "CryptoScamDB"],
							["{}", "Cryptscam"],
							["{}", "SeeKoin"]
						]
					]
				}
			]
		}
	]
}